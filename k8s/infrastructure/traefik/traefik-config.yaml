  GNU nano 7.2                                                             k8s/infrastructure/traefik/traefik-config.yaml                                                                      
apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    deployment:
      replicas: 1

    persistence:
      enabled: true
      name: data
      accessMode: ReadWriteOnce
      size: 128Mi
      path: /data
      storageClass: local-path

    podSecurityContext:
      fsGroup: 65532
      fsGroupChangePolicy: OnRootMismatch

    certificatesResolvers:
      letsencrypt:
        acme:
          email: praktikant@alderis.de
          storage: /data/acme.json
          keyType: EC256
          httpChallenge:
            entryPoint: web
