apiVersion: v1
kind: Service
metadata:
  name: gastro-api-service
  namespace: gastro-api
  labels:
    app: gastro-api
spec:
  selector:
    app: gastro-api
  ports:
  - port: 80
    targetPort: 8080
    name: http
  type: ClusterIP